% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inat_fetch.R
\name{inat_fetch}
\alias{inat_fetch}
\title{Fetch observations from the iNaturalist API}
\usage{
inat_fetch(
  taxon_id,
  place_id = NULL,
  user_login = NULL,
  annotation = NULL,
  quality_grade = "any",
  year = NULL,
  month = NULL,
  licensed = NULL,
  per_page = 200,
  verbose = TRUE
)
}
\arguments{
\item{taxon_id}{Integer. iNaturalist taxon ID (e.g. \code{488444} for
\emph{Caiophora chuquitensis}).}

\item{place_id}{Integer or \code{NULL}. iNaturalist place ID (e.g. \code{6783} for
Bolivia).}

\item{user_login}{Character or \code{NULL}. iNaturalist username.}

\item{annotation}{Character vector of annotation labels, or a single integer
vector \code{c(term_id, term_value_id)}, or \code{NULL}. Use labels from
\link{inat_annotations} (e.g. \code{"flowers"}, \code{"green_leaves"}, \code{"alive"}).
Multiple labels can be passed as \code{c("flowers", "green_leaves")} — each
is fetched separately and results are combined.
See \link{inat_annotations} for all valid labels.}

\item{quality_grade}{Character. One of \code{"research"}, \code{"needs_id"}, or
\code{"any"} (default).}

\item{year}{Integer or \code{NULL}. Filter by observation year.}

\item{month}{Integer (1–12) or \code{NULL}. Filter by observation month.}

\item{licensed}{Logical or \code{NULL}. If \code{TRUE}, return only observations with
a CC photo license.}

\item{per_page}{Integer. Results per API page (max 200).}

\item{verbose}{Logical. Print progress messages (default \code{TRUE}).}
}
\value{
A data frame of observations with list-column \code{photos}.
}
\description{
Retrieves all observations matching the given filters, handling pagination
automatically. Multiple annotations can be passed as a character vector;
each is fetched separately and the results combined.
}
\examples{
\dontrun{
# Single annotation
obs <- inat_fetch(taxon_id = 488444, place_id = 6783,
                  annotation = "flowers")

# Multiple annotations
obs <- inat_fetch(taxon_id = 51935, place_id = 6857,
                  annotation = c("flowers", "green_leaves"))

# See all available annotation labels
inat_annotations
}

}
\seealso{
\link{inat_annotations} for all annotation labels and IDs.
}
